<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
    <style>
      body {
        background-image: url(https://i.imgur.com/2HRRoa7.jpg);
        background-position:bottom;
        background-repeat:no-repeat;
        background-size: 50%;
        background-attachment: fixed;
      }
      .margin {
        margin-left:5px;
      }
    </style>
  </head>
  <body>
    <div class="margin">
    <h1>All Characters</h1>
    <% @characters.sort_by{|c|c.name}.each do |character| %>
      <% if character.movies != [] || character.name != "" || character.name != nil%>
      <% character.movies.uniq.each do |m| %>
      <strong><%= link_to character.name, character %></strong>
      - <u><%= link_to m.name, m %></u>
      Played by: <% character.actors.each do |actor| %> <%= link_to actor.name, actor %><br>
            <% end %>
          <% end %>
        <% else %>
        <br>
      <% end %>
    <% end %><br>
    </div>
  </body>
</html>
