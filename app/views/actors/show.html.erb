<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <meta charset="utf-8">
  <title></title>
  <style>
    .margin {
      margin-left: 5px;
    }
  </style>
</head>

<body>
  <div class="margin">

    <% @full_actor_profile = @actor.get_actor_bio %>
      <img style="padding-left: 30px;padding-top: 30px" src="<%= @actor.img %>">
      <h1>
        <%= @full_actor_profile.name %>
      </h1>
      <p>
        <%= @full_actor_profile.bio %>
      </p>
      <% @full_actor_profile.characters.each do |character| %>
        <%= link_to character.name, character %> in:
          <br>
          <% character.movies.uniq.map do |movie| %>
            <%= link_to movie.name, movie %>
              <br>
              <% end %>
                <br>
                <% end %>
  </div>
</body>

</html>